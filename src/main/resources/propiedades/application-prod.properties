# ================================
# PROD - MariaDB real
# ================================
spring.datasource.url=jdbc:mariadb://localhost:3306/db_rest
spring.datasource.username=root
spring.datasource.password=pj123456
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# ================================
# JPA / HIBERNATE
# ================================
# NO modificar esquema en producción.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# No ejecutar scripts de inicialización:
spring.sql.init.mode=never

# ================================
# RESULTADO PROD
# ================================
# ✔ La base de datos permanece estable (sin DROP/CREATE).
# ✔ No se ejecutan scripts automáticos.
# ✔ Solo las migraciones (Flyway/Liquibase) deben modificar el esquema.
# ✔ Ambiente seguro para datos reales.
# ================================
